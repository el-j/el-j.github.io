# Orchestration
- Use explore agent to map data flow (done)
- Implement metadata + imagery changes directly in repo (done)
- Run targeted tests (fetch-projects, component smoke) (done)
- Capture screenshot after UI updates (done)
